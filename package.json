{
  "name": "cursor06",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "lint": "cd frontend-client && pnpm lint",
    "type-check": "cd frontend-client && pnpm vue-tsc --noEmit",
    "test:unit": "echo \"No tests configured\" && exit 0",
    "build": "cd frontend-client && pnpm build"
  },
  "dependencies": {
    "pinia-plugin-persistedstate": "^4.2.0"
  }
}
